wandb:
  _target_: lightning.pytorch.loggers.wandb.WandbLogger
  name: ${task_name}
  save_dir: "${paths.output_dir}"
  offline: False
  id: null
  anonymous: null
  project: TEST_PROJECT
  log_model: false
  prefix: ""
  # Use currently logged-in account's default entity to avoid permission mismatch.
  entity: se99an
  group: ""
  tags: []
  job_type: ""
  resume: allow
