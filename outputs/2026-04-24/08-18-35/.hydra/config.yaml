checkpoint: /home2/pnc2/repos_python/project_2/logs/pretrained/epoch_last.ckpt
output_dir: data/godfm_pairs
n_rollout_collect: 4
inpaint_steps: 10
goal_weight: 5.0
chunk_size: 5000
data:
  train_batch_size: 4
  val_batch_size: 4
  num_workers: 4
  prefetch_factor: null
  shuffle: true
  _target_: src.smart.datamodules.MultiDataModule
  test_batch_size: 4
  pin_memory: true
  persistent_workers: true
  train_raw_dir: ${paths.cache_root}/training
  val_raw_dir: ${paths.cache_root}/validation
  val_tfrecords_splitted: ${paths.cache_root}/validation_tfrecords_splitted
  test_raw_dir: ${paths.cache_root}/testing
  train_max_num: 32
paths:
  root_dir: ${hydra:runtime.cwd}
  log_dir: ${paths.root_dir}/logs/
  output_dir: ${hydra:runtime.output_dir}
  cache_root: /home2/pnc2/repos_python/datasets/smart_data/waymo_processed_catk_rebuild_parallel_v1
